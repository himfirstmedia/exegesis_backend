# Bible XML files are committed directly (no Git LFS) so Railway's deploy
# includes the real content. They are a bounded set (~150MB total) that
# previously broke production when LFS pointers shipped instead of the files.
